import Vue from 'vue';
import Vuex from 'vuex';

import ches from '@/store/modules/ches.js';

Vue.use(Vuex);

export default new Vuex.Store({
  modules: { ches },
  state: {},
  mutations: {},
  actions: {}
});
